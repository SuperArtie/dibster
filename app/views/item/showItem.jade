extends ../shared/template
block content
  .row
    .col-xs-12
      h2: a(href='')= user.profile
      h5=
  .row
    .col-xs-2
      a.thumbnail(href='#')
        img(src=user.photo)
    .col-xs-10

  .row
    .col-xs-8
      #map
    .col-xs-3
  .row
    .col-xs-12
      table.table.table-striped.table-hover
        thead
          tr
            th Item Name
            th Item Description
            th Location
            th Category
            th Tags
        tbody
          tr
            td= item.name
            td=item.description
               img(src=item.photo)
            td= user.Location1
            td=item.category
            td=item.tags
    .row
      form(method='post', action='')
        button(type='submit', class='btn btn-default') Bid

block scripts
